# 📝 插件更新说明

## 🎯 已针对您的学习平台优化！

根据您提供的页面结构，我已经更新了插件代码，现在应该能够正确识别和处理您的学习平台了。

---

## ✨ 更新内容

### 1. **优化课程卡片识别**

已添加对您平台特定结构的支持：

**新增选择器（优先级最高）：**
```css
.item.hover-shadow   /* 您的平台课程卡片 */
.item                /* 通用备选 */
```

**新增特征检测：**
- ✅ `.cover` - 课程封面
- ✅ `.info-box` - 信息框
- ✅ `.speaker` - 主讲人信息
- ✅ `.progress` - 学习进度

### 2. **改进课程标题获取**

现在会按以下优先级获取课程标识：

1. **主讲人信息** - `.speaker .c-text`（例如：主讲人：韩庆祥）
2. **图片URL** - `img.src`（唯一标识）
3. **学时信息** - `.cover-box span`（学时数等）
4. **其他标题元素**

### 3. **优化课程ID生成**

使用图片URL作为唯一标识，确保不会重复学习同一课程：

```javascript
// 优先使用图片URL（最可靠）
img.src → "https://uploadcdn.scgb.gov.cn/filedownload/..."
```

### 4. **增强调试功能**

添加了详细的日志输出，方便排查问题：

```
✅ 使用选择器 ".item.hover-shadow" 找到 12 个课程
📚 准备学习: 主讲人：韩庆祥
🎥 检测到视频页面
⚡ 已设置视频倍速: 1.5x
```

---

## 🚀 如何使用更新后的插件

### 方法1：重新加载插件（推荐）

1. 打开 `chrome://extensions/` 或 `edge://extensions/`
2. 找到"自动学习助手"
3. 点击 **刷新图标** ⟳ 或 **重新加载**
4. 完成！

### 方法2：完全重装

1. 在扩展管理页面移除旧版本
2. 点击"加载已解压的扩展程序"
3. 选择插件文件夹
4. 完成！

---

## 🧪 测试步骤

### 第一步：使用调试工具

1. **打开调试工具**
   - 在浏览器中打开 `调试工具.html`

2. **检测课程卡片**
   - 打开您的学习平台
   - 按 F12 打开开发者工具
   - 复制"检测课程卡片"代码到 Console 运行
   - 查看是否能识别到课程（会用红色边框高亮）

3. **模拟自动化**
   - 复制"模拟自动化流程"代码
   - 在 Console 运行
   - 观察是否正确识别并标记课程

### 第二步：测试插件功能

1. **打开课程列表页**
   ```
   确保页面显示多个课程卡片
   ```

2. **打开插件面板**
   ```
   点击浏览器工具栏的插件图标
   ```

3. **查看日志**
   ```
   插件面板会显示：
   - 找到多少个课程
   - 使用哪个选择器
   - 准备学习哪个课程
   ```

4. **开始测试**
   ```
   点击"开始学习"按钮
   观察插件是否：
   ✓ 识别到课程列表
   ✓ 点击第一个课程
   ✓ 跳转到视频页面
   ✓ 自动播放视频
   ✓ 设置正确的倍速
   ```

---

## 🔍 预期行为

### 在课程列表页：

```
[日志] 开始自动学习
[日志] 使用选择器 ".item.hover-shadow" 找到 8 个课程
[日志] 检测到课程列表页面，共 8 个课程
[日志] 准备学习: 主讲人：韩庆祥
[日志] 点击课程卡片
→ 自动跳转到课程详情页
```

### 在视频播放页：

```
[日志] 检测到视频页面
[日志] 已设置视频倍速: 1.5x
[日志] 视频开始播放
→ 视频自动播放（1.5倍速）
→ 播放完成后返回列表
```

### 完成一个课程后：

```
[日志] 视频播放完成
[日志] 准备返回课程列表...
→ 2秒后自动返回
→ 继续学习下一个课程
```

---

## ⚠️ 可能遇到的问题

### 问题1：仍然识别不到课程

**检查步骤：**

1. 打开 F12 → Console
2. 查看是否有错误信息
3. 运行调试工具中的检测代码
4. 截图页面HTML结构

**临时方案：**

手动在 Console 运行：
```javascript
document.querySelectorAll('.item.hover-shadow')
```
如果返回空数组，说明选择器不匹配

### 问题2：点击后没反应

**原因：**
- 课程卡片可能不可点击
- 需要点击内部的链接或按钮

**解决：**

在 Console 测试：
```javascript
const card = document.querySelector('.item.hover-shadow');
card.click(); // 尝试直接点击
```

或者：
```javascript
const link = card.querySelector('a');
if (link) link.click(); // 点击内部链接
```

### 问题3：视频不自动播放

**原因：**
浏览器自动播放策略限制

**解决：**
1. 手动点击播放一次
2. 之后会自动播放

### 问题4：返回列表失败

**原因：**
页面可能没有返回按钮

**当前处理：**
插件会使用 `history.back()` 返回上一页

**检查：**
确保课程页面可以通过浏览器后退返回列表

---

## 📊 调试方法

### 查看插件日志

**方法1：在插件面板查看**
- 点击插件图标
- 查看底部的"运行日志"区域

**方法2：在开发者工具查看**
```
F12 → Console → 搜索 "[自动学习助手]"
```

### 查看存储数据

在 Console 运行：
```javascript
chrome.storage.local.get(null, (data) => {
  console.log('插件存储数据:', data);
});
```

应该看到：
```javascript
{
  isRunning: true/false,
  learnedCount: 数字,
  learnedCourses: ["课程1", "课程2", ...],
  videoSpeed: "1.5",
  autoNext: true,
  loopLearning: true
}
```

---

## 🛠️ 高级自定义

如果插件仍然无法正常工作，您可以手动调整 `content.js`：

### 修改选择器

找到第 211 行：
```javascript
const selectors = [
  '.item.hover-shadow',          // 您的平台
  '.your-custom-selector',       // 添加您的选择器
  // ...
];
```

### 修改点击逻辑

找到第 301 行的 `clickCourseCard` 函数：
```javascript
function clickCourseCard(card) {
  // 添加自定义点击逻辑
  const customButton = card.querySelector('.your-button-class');
  if (customButton) {
    customButton.click();
    return;
  }
  
  // 原有逻辑...
}
```

---

## 📞 需要帮助？

如果测试后仍有问题，请提供：

1. **浏览器Console截图**（按F12打开）
2. **插件日志截图**（插件面板底部）
3. **调试工具运行结果**
4. **课程卡片HTML代码**（右键→检查元素）

我可以进一步调整代码！

---

## ✅ 更新文件清单

已修改的文件：
- ✅ `content.js` - 核心逻辑优化

新增的文件：
- ✅ `调试工具.html` - 调试辅助工具
- ✅ `更新说明.md` - 本文件

---

**祝您使用愉快！如有问题随时反馈。📚✨**

