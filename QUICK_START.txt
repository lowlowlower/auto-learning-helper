========================================
  🚀 超级简单 - 一键推送到GitHub
========================================

只需要2步：

1. 双击运行：一键推送.bat

2. 等待完成（会自动打开浏览器查看仓库）

就这么简单！✅

========================================
或者手动执行命令：
========================================

在当前目录打开PowerShell，然后运行：

git init
git add .
git commit -m "🎉 自动学习助手"
gh repo create auto-learning-helper --public --source=. --remote=origin --push

========================================

